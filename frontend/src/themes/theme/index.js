import LayoutAccent from './layoutAccent';

// ==============================|| PRESET THEME - THEME SELECTOR ||============================== //

const Theme = (presetColor, mode) => LayoutAccent(presetColor, mode);

export default Theme;
