/**
 * Legacy compatibility page.
 * Product Colors are now managed inside each BOM and this page is intentionally
 * not registered in the active router/menu. Keeping the file makes an in-place
 * source-folder replacement safe when an older checkout still contains the route.
 */
export default function ProductColorPage() {
  return null;
}
